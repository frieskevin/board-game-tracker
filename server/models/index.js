const User = require('./User')
const Game = require('./Game')
const GamesPlayed = require('./GamesPlayed')
const Characters = require('./Character')

module.exports = { User, Game, GamesPlayed, Characters }